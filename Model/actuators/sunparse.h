#ifndef SUNPARSE
#define SUNPARSE

#include "sun.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#endif

static char* xml_find (char* buf, const char* tag);
void sun_parse_data (sun_t* this);
